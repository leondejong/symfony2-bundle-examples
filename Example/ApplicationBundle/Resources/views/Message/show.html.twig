{% extends 'ExampleApplicationBundle:Layout:main.html.twig' %}
{% block header %}
<h1>Show Message</h1>
{% endblock header %}
{% block local %}
{% include "ExampleApplicationBundle:Message:navigation.html.twig" %}
{% endblock local %}
{% block content %}
{% block read %}
<table class="read">
    <tbody>
        <tr>
            <th>User</th>
            {% if entity.user is empty %}
            <td>-</td>
            {% else %}
            <td>{{ entity.user.username }}</td>
            {% endif %}
        </tr>
        <tr>
            <th>Category</th>
            {% if entity.category is empty %}
            <td>-</td>
            {% else %}
            <td>{{ entity.category.title }}</td>
            {% endif %}
        </tr>
        <tr>
            <th>Page</th>
            {% if entity.page is empty %}
            <td>-</td>
            {% else %}
            <td>{{ entity.page.title }}</td>
            {% endif %}
        </tr>
        <tr>
            <th>Item</th>
            {% if entity.item is empty %}
            <td>-</td>
            {% else %}
            <td>{{ entity.item.title }}</td>
            {% endif %}
        </tr>
        <tr>
            <th>Object</th>
            {% if entity.object is empty %}
            <td>-</td>
            {% else %}
            <td>{{ entity.object.title }}</td>
            {% endif %}
        </tr>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ entity.type }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ entity.status }}</td>
        </tr>
        <tr>
            <th>Position</th>
            <td>{{ entity.position }}</td>
        </tr>
        <tr>
            <th>Data</th>
            <td>{{ entity.data }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Change</th>
            <td>{{ entity.change|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ entity.title }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ entity.content }}</td>
        </tr>
        <tr>
            <th>Views</th>
            <td>{{ entity.views }}</td>
        </tr>
        <tr>
            <th>Marks</th>
            <td>{{ entity.marks }}</td>
        </tr>
    </tbody>
</table>
{% endblock read %}
{% block update %}
<div class="update">
    <a href="{{ path('message_edit', { 'id': entity.id }) }}">
        Edit Message
    </a>
</div>
{% endblock update %}
{% block delete %}
<form action="{{ path('message_delete', { 'id': entity.id }) }}" method="post" class="delete" novalidate>
    {{ form_widget(delete_form) }}
    <button type="submit">Delete Message</button>
</form>
{% endblock delete %}
{% endblock content %}